apiVersion: apps/v1
kind: Deployment
metadata:
  name: openfloat-erp
  namespace: openfloat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: openfloat-erp
  template:
    metadata:
      labels:
        app: openfloat-erp
    spec:
      containers:
        - name: openfloat-erp
          image: openfloat/openfloat-erp:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: openfloat-config
            - secretRef:
                name: openfloat-secrets
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits: { cpu: "1", memory: 1Gi }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8082 }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8082 }
            initialDelaySeconds: 60
            periodSeconds: 20
